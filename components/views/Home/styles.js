import { StyleSheet } from 'react-native';

export default StyleSheet.flatten({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  redText: {
    color: 'red'
  },
});
